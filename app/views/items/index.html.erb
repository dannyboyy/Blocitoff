<h1>All Items</h1>
<% @items.each do |item| %>
   <div class="media">
     <div class="media-body">
       <h4 class="media-heading">
         <%= item.body %>
         <%= item.days_left %>
         <%= item.complete %>
       </h4>
     </div>
   </div>
<% end %>
